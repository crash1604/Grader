#ifndef EXISTS_H
#define EXISTS_H
#include<iostream>
#include<string>
#include<fstream>
#include <sstream>

using namespace std;
 bool exists(const string& name);
 void fileCheck(const string& file);
 bool is_number(const string& s);
 bool isWord(const string &str);
 bool wdr(const string &temp);



#endif
